type: object
additionalProperties: false
required:
  - enabled
  - shallow
  - inline-source
  - exclude
  - parsing
properties:
  enabled:
    type: boolean
  shallow:
    type: boolean
  inline-source:
    type: boolean
    nullable: true
  exclude:
    $ref: exclude-cooked
  source-type:
    $ref: source-type
  parsing:
    $ref: parsing
