type: object
additionalProperties: false
required:
  - type
  - track
  - configuration
properties:
  type:
    const: start
  track:
    type: string
  configuration:
    $ref: configuration-internal
