default:
  test: null
  prod: null
dependencies:
  process:
    - assert
    - util
    - log
    - specifier
    - configuration
    - agent
  batch:
    - assert
    - log
    - util
    - spawn
    - configuration
    - server
