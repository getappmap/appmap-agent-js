default:
  test: null
  prod: null
dependencies:
  node:
    - assert
    - util
    - log
    - specifier
    - configuration
    - agent
  batch:
    - assert
    - util
    - configuration
    - server
