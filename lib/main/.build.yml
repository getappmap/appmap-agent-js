default:
  test: null
  prod: null
dependencies:
  node:
    - assert
    - util
    - log
    - specifier
    - configuration
    - agent
  batch:
    - assert
    - log
    - util
    - spawn
    - configuration
    - server
