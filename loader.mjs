/* global APPMAP_TRANSFORM_SOURCE */

export const transformSource = (content, context, transformSource) => {
  console.log("YO", content, context);
  return transformSource(content, context, transformSource);
};

exports;
