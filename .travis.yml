arch: amd64
os: linux
dist: focal
language: node_js
cache: npm
node_js: 16

install:
  - npm ci
  - npm run pre-build
  - npm run build

stages:
  - lint
  - test
  - deploy

jobs:
  include:
    - stage: test
      os: windows
      node_js: 14.0.0
      script:
        - node components/recorder-cli/default/index.test.mjs
