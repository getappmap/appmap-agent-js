default:
  test: null
  prod: null
dependencies:
  process:
    - util
    - expect
    - log
    - specifier
    - configuration
    - agent
  batch:
    - util
    - expect
    - log
    - spawn
    - configuration
    - server
