import { noop } from "../../util/index.mjs";

export const validateSerial = noop;
export const validateMessage = noop;
export const validatePayload = noop;
export const validateExternalConfiguration = noop;
export const validateInternalConfiguration = noop;
export const validateSourceMap = noop;
